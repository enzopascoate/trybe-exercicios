{
  "name": "exercicio",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "env $(cat .env) nodemon src/server.js",
    "migrate": "env $(cat .env) npx sequelize-cli db:migrate",
    "migrate:undo": "env $(cat .env) npx sequelize-cli db:migrate:undo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1",
    "mysql2": "3.0.0",
    "nodemon": "^3.1.0",
    "sequelize": "6.3.4"
  },
  "devDependencies": {
    "sequelize-cli": "6.6.1"
  }
}
